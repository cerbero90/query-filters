suites:
    main:
        spec_path: tests
        namespace: Cerbero\QueryFilters
        psr4_prefix: Cerbero\QueryFilters
extensions:
    - PhpSpec\Extension\CodeCoverageExtension
code_coverage:
    format:
      - clover
    output:
      clover: coverage.clover
